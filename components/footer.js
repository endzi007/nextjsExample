import React from 'react'
import footerStyles from '../styles/Footer.module.css';

export default function Footer() {
    return (
        <div className={`${footerStyles.footer} padding`}>
            this is footer
        </div>
    )
}
